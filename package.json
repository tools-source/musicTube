{
  "name": "musictube-static-site",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "serve": "^14.0.0"
  }
}
